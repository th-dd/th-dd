---
import { useTastro } from "@sipc.ink/tastro"
const { t } = useTastro(Astro.request)
---

<div class="glass-card" style="width: 100%; height: 100%; padding: 20px; background: rgba(20, 20, 20, 0.65); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); border-radius: 16px; border: 1px solid rgba(255, 255, 255, 0.08);">
    <h4 style="margin-bottom: 12px; font-size: 16px;">⏰ {t("time.title")}</h4>
    <div id="time-display" style="font-size: 2em; font-weight: bold; text-align: center;">
        Loading...
    </div>
    <script>
        function updateTime() {
            const now = new Date();
            const options = { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false };
            document.getElementById('time-display').textContent = now.toLocaleTimeString('zh-CN', options);
        }
        updateTime();
        setInterval(updateTime, 1000);
    </script>
</div>
